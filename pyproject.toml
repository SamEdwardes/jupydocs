[tool.poetry]
name = "jupydocs"
version = "0.1.0"
description = "Easy Python package documentation using jupyter"
authors = ["SamEdwardes <edwardes.s@gmail.com>"]
license = "MIT"

[tool.poetry.dependencies]
python = "^3.7"
# need pandas 1.1. for tabluate index argmument in df.tomarkdown

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
